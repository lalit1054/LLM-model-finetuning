You are an expert ML engineer. I have fine-tuned a small language model using QLoRA (Unsloth).

Model path:
`model/qwen3.5-0.8b-chatdoctor-unsloth`

Task:
Domain-specific Q&A (medical chatbot style) on chatDoctor 7K data

Data path:
data/processed


Now I want to perform **complete evaluation and analysis** as per a professional ML assignment.

Your job is to generate:

1. Clean, modular Python code (Jupyter Notebook style)
2. Proper evaluation pipeline
3. Visualization + analysis
4. Comparison with base model
5. Error analysis

---

### STEP 1: LOAD MODELS

---

* Load both:

  1. Base model Qwen 0.8B (from 'models/base')
  2. Fine-tuned QLoRA model (from 'model/qwen3.5-0.8b-chatdoctor-unsloth')

* Use HuggingFace Transformers + PEFT or Unsloth
* Ensure inference works on CPU (low memory optimized)

---

### STEP 2: LOAD TEST DATA

---

* Assume dataset format:
  [
  {
  "instruction": "...",
  "input": "...",
  "output": "..."
  }
  ]

* Load test split only

* Create evaluation loop

---

### STEP 3: GENERATE PREDICTIONS

---

For each test sample:

* Generate response from:

  1. Base model
  2. Fine-tuned model

Store:

* ground truth
* base prediction
* finetuned prediction

---

### STEP 4: METRICS (IMPORTANT)

---

Implement at least 2–3 metrics:

1. ROUGE (rouge1, rouge2, rougeL)
2. BLEU score
3. Optional: BERTScore (if lightweight)

Return:

* Average scores for base model
* Average scores for fine-tuned model

---

### STEP 5: VISUALIZATION

---

* Plot:

  1. Metric comparison (bar chart)
  2. If training logs available → loss curves

Use matplotlib only

---

### STEP 6: QUALITATIVE ANALYSIS

---

Print 10 examples:

* Instruction
* Ground truth
* Base output
* Fine-tuned output

Highlight:

* Where fine-tuned model improved
* Where it failed

---

### STEP 7: ERROR ANALYSIS

---

Automatically categorize errors:

* Hallucination
* Incomplete answer
* Wrong medical info
* Repetition

Print summary:

* % of each error type

---

### STEP 8: OVERFITTING CHECK

---

* Compare train vs validation loss (if logs exist)
* Comment:

  * Is model overfitting?
  * Evidence

---

### STEP 9: SAVE RESULTS

---

Save:

* predictions.json
* metrics.json
* evaluation_report.txt

---

### STEP 10: BONUS (IMPORTANT)

---

Add:

* Prompt-based baseline (no fine-tuning)
* Compare with fine-tuned model

---

### OUTPUT REQUIREMENTS

---

* Clean notebook cells
* Comments explaining each step
* Functions modular (generate(), evaluate(), etc.)
* Optimized for CPU

---

### STYLE

---

* Production-quality code
* No unnecessary verbosity
* Clear markdown explanations

---

IMPORTANT:
This is for a technical assignment, so structure output professionally and clearly.

